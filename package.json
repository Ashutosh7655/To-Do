{
  "name": "to-do",
  "version": "1.0.0",
  "description": "A To Do app which uses sqllite db and jwt for authorization",
  "main": "index.js",
  "type":"module",
  "scripts": {
    "start":"npx nodemon --env-file=.env ./src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Ashutosh",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2"
  }
}
